<%= simple_form_for @experience do|f| %>
	<%= f.input :title %>
	<%= f.input :description %>
	<%= f.input :company %>
	<%= f.input :starts_at %>
	<%= f.input :ends_at %>
	<%= f.input :logo, as: :attachinary %>

	<%= f.button :submit %>
<% end %>